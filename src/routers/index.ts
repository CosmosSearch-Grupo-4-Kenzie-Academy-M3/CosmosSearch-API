import users from "./users.router";
import likes from "./likes.router";
import posts from "./posts.router";
import comments from "./comments.router";

const routes = {
    users,
    likes,
    posts,
    comments
}

export default routes